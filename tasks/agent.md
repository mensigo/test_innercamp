# Постановка задачи

## Общее

Пишем простого агента по теме обучения в вузе (студенты, предметы, успеваемость и тд).  
Можно сказать, что студенты уже прошли экзамены и находятся на каникулах.

Агент должен уметь отвечать на некоторые типовые вопросы. Проверка осуществляется через набор end-to-end тестов.

## Глобальная цель

Написать функцию agent, которая будет принимать на вход запрос пользователя и возвращать ответ агента (строка + экстра инфо). Подробнее см. в секции "Что нужно реализовать".

## Что дано

- данные
- апи к ним
- тесты

Предполагается, что агент сможет извлекать интент/параметры из запроса пользователя и запрашивать необходимые данные через существующие апи.

### Данные

Структура:

```
src/data
├── courses
│   ├── ml_bayes.md
│   ├── ml_extra.md
│   ├── ml_intro.md
│   ├── ml_practics.md
│   ├── ml.md
│   ├── opt_cont.md
│   ├── opt_ml.md
│   ├── opt.md
│   ├── phil.md
│   ├── prob_stats.md
│   ├── prob.md
│   └── rand_proc.md
└── students.csv
```

Общее:

Всего 30 студентов, каждый прошел 2 предмета и имеет оценку по каждому. Эти данные представлены в файле students.csv (колонки student_name,subject_name,score).

Первые строки students.csv:

```
student_name,subject_name,score
Алексеева Елена,Machine Learning,4.3
Алексеева Елена,Probability Theory,3.3
Андреев Матвей,Probability Theory,3.3
Андреев Матвей,Optimization Theory,4.9
```

В данных students.csv всего 3 предмета: Machine Learning, Probability Theory, Optimization Theory.

В директории courses представлены .md файлы с описанием курсов: 3 из них соответствуют предметам выше, остальные 9 - похожи на них, но напрямую не связаны.

### API

Представлены в виде python функций в директории src/api.

#### 1. get_top_students

- на вход название предмета и топ-k (дефолт k=3)
- возвращает информацию топ-k студентов, отсортированных по баллу (при равном балле - по имени)
- пример запроса: `get_top_students("Machine Learning", k=2)`
- пример ответа: `[ {"name": "Васильев Артем", "score": 4.9}, {"name": "Павлова Ирина", "score": 4.7} ]`
- !! если название предмета пусто / нет в данных, то возвращает пустой список
- !! если значение k < 1, то возвращает пустой список
- !! если значение k > 10, то падает с ошибкой

#### 2. get_avg_score

- на вход название предмета
- возвращает средний балл студентов по указанному предмету
- пример запроса: `get_avg_score("Probability Theory")`
- пример ответа: `{ "avg_score": 4.1 }`
- !! если название предмета пусто / нет в данных, то возвращает пустой словарь

#### 3. get_avg_overall_score

- на вход ничего
- возвращает средний балл студентов по всем предметам
- пример запроса: `get_avg_overall_score()`
- пример ответа: `{ "avg_score": 4.0 }`

#### 4. vector_search

- на вход строка-запрос и топ-k (дефолт k=2)
- возвращает набор чанков
- пример запроса: "лектор по мл"
- пример ответа: { "chunks":  [ "text1", "text2" ] }

### Тесты

Дан набор тестов (директория test/agent), которые должен проходить агент. Тесты имеют вид end-to-end: запрос пользователя (строка) -> ответ.

Список тестов:

```
test_agent_irrelevant.py             # нерелевантные запросы
test_agent_get_top_students.py       # агент -> api 1
test_agent_get_avg_score.py          # агент -> api 2
test_agent_get_avg_overall_score.py  # агент -> api 3
test_agent_vector_search.py          # агент -> api 4
...
```

Также есть базовые тесты на проверку api, вызовов llm, их общей конфигурации (должны проходить всегда):

```
test
├── api
│   ├── test_get_avg_overall_score.py
│   ├── test_get_avg_score.py
│   ├── test_get_top_students.py
│   └── test_vector_search.py
├── llm
│   └── test_utils.py
├── test_prepare_data_db.py
└── test_prepare_data_index.py
```

## Что нужно реализовать

### Агент

В директории src есть agent.py с функцией agent - ее нужно написать. В ней зашита вся механика с вызовом API, принятием решений и тд.

Пример запроса пользователя:

```python
user_request = "топ студентов по мл"
```

Пример ответа функции agent:

```python
agent_result = {
    "answer": "...",
    "token_usage": {
        "prompt_tokens": 18,
        "completion_tokens": 24,
        "total_tokens": 42
    }
}
```

Добавление token_usage пока опционально.

### Эмбеддинги

Работа API vector_search завязана на индексе, для построения которого нужны эмбеддинги.

В директории src есть utils.py с функцией get_embeddings - ее нужно реализовать. Затем запустить src/prepare_data.py для построения индекса.

Пример запроса для `get_embeddings`:

```python
{'input': 'машинное обучение'}
```

Пример ответа (для размерности эмбеддингов d=2):

```json
[0.935546875, -0.092529296]
```

## Общие требования

- ходить за данными можно только через API
- для работы API vector_search нужно построить индекс по .md документам

## На усмотрение разработчика (не проверяется тестами)

- логирование, доп. пакеты питона
- использование LLM API на генерацию (gigachat, локальные модели)
- ответы функции agent(), явно не прописанные в задании/тестах (например, если в запросе на топ студентов нет основного предмета: "топ студентов по философии")


# Начало работы (пример)

1. Установить питон (sberusersoft). Тестировалось на версии 3.11

2. Установить пакеты из pyproject.toml (можно создать вирт. окружение, можно ставить глобально)
  2.1 Настроить index-url (sberosc). Подробнее см. [sigma_sberosc.md](sigma_sberosc.md)
  2.2 Поставить пакеты через `pip install .` (advanced: можно через `uv sync`, если настроить uv)

3. Подготовить индекс с markdown документами
  3.1 Написать реализацию функции `get_embeddings` в `src/utils.py`
  3.2 Проверить через `pytest -m llm` (см. метки в pyproject.toml)
  3.3 Построить индекс `python src/prepare_data.py`

4. Прогнать тесты
  4.1 Базовые функции: `pytest -m "api or unit"` (должны пройти успешно)
  4.2 Функция agent: `pytest -m agent` (должны упасть, тк функция agent() еще не реализована)

Теперь можно начинать добавлять логику агента в src/agent.py, ориентируясь на кейсы из тестов test/agent.
**Критерий** прохождения задания: все тесты из директории test/ проходят успешно (запуск `pytest`).


# Полезные материалы

- Пример вызова API GigaChat на ИФТ с сертификатами [sigma_gigachat.md](sigma_gigachat.md)
- Официальная документация API GigaChat [(ссылка)](https://developers.sber.ru/docs/ru/gigachat/api/reference/rest/post-chat)
- Форматтер кода ruff (команда `ruff format`)